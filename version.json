{
  "version": "2.7.3",
  "release_date": "2026-09-15",
  "changelog": "fix: update checker compared version strings for inequality, so it announced an 'update' whenever local and remote differed — including when the local build was NEWER, the normal state on a dev machine between deploying and pushing. Versions are now compared numerically (which also fixes 2.7.10 being treated as older than 2.7.9), and an update is only offered when the remote is genuinely ahead."
}
